import { css } from "@emotion/react";

// label is to make sure css is understandable in inspector
export const headerWrapper = css`
	color: red;
	font-family: 'DM Sans', sans-serif;
	label: headerWrapper;
`